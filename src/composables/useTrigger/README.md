# useTrigger

```typescript
import {triggerProps} from '@/composables/useTrigger';

const props = defineProps({
  ...triggerProps,
});
```

## Props

### trigger

#### Default Value

`['hover', 'focus']`

#### Available Values

- `click`
- `hover`
- `focus`
- `manual`
